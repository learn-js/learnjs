# Introduction to testing

In this book we will test code using [tap](https://github.com/isaacs/node-tap), [tape](https://github.com/substack/tape), and [mocha](https://github.com/visionmedia/mocha).

## tap

## tape

## mocha

### install:

~~~~~~~~
npm install -g mocha
~~~~~~~~

### Example:

~~~~~~~~
var assert = require("assert");

describe('Array', function(){
  describe('#indexOf()', function(){
    it('should return -1 when the value is not present', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    });
  });
});
~~~~~~~~

### Run the example with the mocha terminal command:

~~~~~~~~
mocha
~~~~~~~~